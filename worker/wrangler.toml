name = "pillpals-push"
main = "worker.js"
compatibility_date = "2024-09-01"

# KV хранилище для подписок и расписания
# ⚠️ id должен совпадать с реальным id namespace "ill-als-pillpals-kv"
# проверить: npx wrangler kv namespace list
[[kv_namespaces]]
binding = "KV_NAMESPACE"
id = "b01028e1be534e1c90fd3ea8a6a48629"

# Cron: каждую минуту
[triggers]
crons = ["* * * * *"]

[vars]
VAPID_SUBJECT = "mailto:xatabeach42@gmail.com"
# Сайт на GitHub Pages (для абсолютного пути к иконке в уведомлении)
SITE_URL = "https://xlop4ikk.github.io/PillPals-dev"
# Публичный VAPID-ключ (не секрет)
VAPID_PUBLIC_KEY = "BGLt0H71qUtSrxBPlacmvhJnIEZNavS_W1OmJeLCtVqzDzQnPX5dKDzxOgdkOPk9KnCMQrc078EHzF2LXaGsqcs"
# Приватный VAPID-ключ. Для личного проекта ок в vars;
# безопаснее: npx wrangler secret put VAPID_PRIVATE_KEY и удалить строку ниже
VAPID_PRIVATE_KEY = "0c6YGdrBebdec6fPXwHeYxE5z7d5DUAgOS1c1zL9ooU"